<Window x:Class="BowlingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BowlingApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="300">
    <Grid>
        <StackPanel Orientation="Vertical" >
            <Label Content="{Binding BindText}" Margin="0,0,0,0" Grid.Row="1"/>
            <Button x:Name="StartButton" Margin="10,10" Content="Start" Command="{Binding StartButtonClickCommand}"/>
        </StackPanel>
    </Grid>
</Window>
