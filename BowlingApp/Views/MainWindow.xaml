<Window x:Class="BowlingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/" xmlns:local="clr-namespace:BowlingApp.Views"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="300" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10,30,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:ScoreBord Grid.Row="0" Grid.ColumnSpan="2"/>

        <Button x:Name="StartButton"
                Content="投げる"
                Command="{Binding ThrowButtonClickCommand}" 
                FontSize="15"
                Grid.Row="1"
                Grid.Column="0"
                Width="100"
                Height="40"/>

        <Button x:Name="ScoreButton"
                Content="スコア"
                Command="{Binding ScoreButtonClickCommand}" 
                FontSize="15"
                Grid.Row="1"
                Grid.Column="1"
                Width="100"
                Height="40"/>
    </Grid>
</Window>
